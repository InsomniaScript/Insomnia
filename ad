local Players = game:GetService("Players")
local TweenService = game:GetService("TweenService")
local UserInputService = game:GetService("UserInputService")

local Player = Players.LocalPlayer
local PlayerGui = Player:WaitForChild("PlayerGui")

local DiscordLink = "https://discord.gg/Nmh2Sjdf74"

-- Creator Store asset
local IMAGE_ID = "rbxthumb://type=Asset&id=9266524146&w=420&h=420"

local OldUI = PlayerGui:FindFirstChild("InsomniaPurchaseUI")

if OldUI then
	OldUI:Destroy()
end

local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "InsomniaPurchaseUI"
ScreenGui.IgnoreGuiInset = true
ScreenGui.ResetOnSpawn = false
ScreenGui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling
ScreenGui.Parent = PlayerGui

-- Overlay
local Overlay = Instance.new("Frame")
Overlay.Size = UDim2.fromScale(1, 1)
Overlay.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
Overlay.BackgroundTransparency = 1
Overlay.BorderSizePixel = 0
Overlay.Parent = ScreenGui

-- Main window
local Main = Instance.new("Frame")
Main.Size = UDim2.fromOffset(500, 285)
Main.Position = UDim2.fromScale(0.5, 0.5)
Main.AnchorPoint = Vector2.new(0.5, 0.5)
Main.BackgroundColor3 = Color3.fromRGB(5, 5, 5)
Main.BorderSizePixel = 0
Main.ClipsDescendants = true
Main.Parent = Overlay

local MainCorner = Instance.new("UICorner")
MainCorner.CornerRadius = UDim.new(0, 18)
MainCorner.Parent = Main

local MainStroke = Instance.new("UIStroke")
MainStroke.Color = Color3.fromRGB(50, 50, 50)
MainStroke.Thickness = 1.5
MainStroke.Transparency = 0.15
MainStroke.Parent = Main

-- Black gradient
local MainGradient = Instance.new("UIGradient")
MainGradient.Color = ColorSequence.new({
	ColorSequenceKeypoint.new(0, Color3.fromRGB(18, 18, 18)),
	ColorSequenceKeypoint.new(0.5, Color3.fromRGB(6, 6, 6)),
	ColorSequenceKeypoint.new(1, Color3.fromRGB(0, 0, 0))
})
MainGradient.Rotation = 35
MainGradient.Parent = Main

-- Top line
local TopLine = Instance.new("Frame")
TopLine.Size = UDim2.new(1, 0, 0, 2)
TopLine.BackgroundColor3 = Color3.fromRGB(150, 150, 150)
TopLine.BorderSizePixel = 0
TopLine.Parent = Main

local TopLineGradient = Instance.new("UIGradient")
TopLineGradient.Color = ColorSequence.new({
	ColorSequenceKeypoint.new(0, Color3.fromRGB(20, 20, 20)),
	ColorSequenceKeypoint.new(0.5, Color3.fromRGB(180, 180, 180)),
	ColorSequenceKeypoint.new(1, Color3.fromRGB(20, 20, 20))
})
TopLineGradient.Parent = TopLine

--------------------------------------------------
-- SZA IMAGE / LOGO
--------------------------------------------------

local LogoHolder = Instance.new("Frame")
LogoHolder.Size = UDim2.fromOffset(58, 58)
LogoHolder.Position = UDim2.fromOffset(25, 25)
LogoHolder.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
LogoHolder.BorderSizePixel = 0
LogoHolder.ClipsDescendants = true
LogoHolder.Parent = Main

local LogoHolderCorner = Instance.new("UICorner")
LogoHolderCorner.CornerRadius = UDim.new(0, 14)
LogoHolderCorner.Parent = LogoHolder

local LogoHolderStroke = Instance.new("UIStroke")
LogoHolderStroke.Color = Color3.fromRGB(85, 85, 85)
LogoHolderStroke.Thickness = 1
LogoHolderStroke.Parent = LogoHolder

local LogoImage = Instance.new("ImageLabel")
LogoImage.Size = UDim2.fromScale(1, 1)
LogoImage.Position = UDim2.fromScale(0, 0)
LogoImage.BackgroundTransparency = 1
LogoImage.BorderSizePixel = 0

-- IMPORTANT
LogoImage.Image = IMAGE_ID

LogoImage.ScaleType = Enum.ScaleType.Crop
LogoImage.Parent = LogoHolder

local LogoImageCorner = Instance.new("UICorner")
LogoImageCorner.CornerRadius = UDim.new(0, 13)
LogoImageCorner.Parent = LogoImage

--------------------------------------------------
-- TITLE
--------------------------------------------------

local Title = Instance.new("TextLabel")
Title.Size = UDim2.new(1, -150, 0, 30)
Title.Position = UDim2.fromOffset(98, 29)
Title.BackgroundTransparency = 1
Title.Text = "INSOMNIA"
Title.TextColor3 = Color3.fromRGB(255, 255, 255)
Title.TextSize = 24
Title.Font = Enum.Font.GothamBold
Title.TextXAlignment = Enum.TextXAlignment.Left
Title.Parent = Main

local Subtitle = Instance.new("TextLabel")
Subtitle.Size = UDim2.new(1, -150, 0, 20)
Subtitle.Position = UDim2.fromOffset(99, 59)
Subtitle.BackgroundTransparency = 1
Subtitle.Text = "Premium Script Hub"
Subtitle.TextColor3 = Color3.fromRGB(120, 120, 120)
Subtitle.TextSize = 13
Subtitle.Font = Enum.Font.GothamMedium
Subtitle.TextXAlignment = Enum.TextXAlignment.Left
Subtitle.Parent = Main

--------------------------------------------------
-- CLOSE
--------------------------------------------------

local Close = Instance.new("TextButton")
Close.Size = UDim2.fromOffset(35, 35)
Close.Position = UDim2.new(1, -51, 0, 19)
Close.BackgroundColor3 = Color3.fromRGB(14, 14, 14)
Close.BorderSizePixel = 0
Close.Text = "×"
Close.TextColor3 = Color3.fromRGB(165, 165, 165)
Close.TextSize = 25
Close.Font = Enum.Font.GothamMedium
Close.AutoButtonColor = false
Close.Parent = Main

local CloseCorner = Instance.new("UICorner")
CloseCorner.CornerRadius = UDim.new(0, 9)
CloseCorner.Parent = Close

local CloseStroke = Instance.new("UIStroke")
CloseStroke.Color = Color3.fromRGB(50, 50, 50)
CloseStroke.Thickness = 1
CloseStroke.Parent = Close

--------------------------------------------------
-- MESSAGE
--------------------------------------------------

local Message = Instance.new("TextLabel")
Message.Size = UDim2.new(1, -54, 0, 45)
Message.Position = UDim2.fromOffset(27, 105)
Message.BackgroundTransparency = 1
Message.Text = "To purchase Insomnia, join this Discord server!"
Message.TextColor3 = Color3.fromRGB(235, 235, 235)
Message.TextSize = 17
Message.Font = Enum.Font.GothamMedium
Message.TextWrapped = true
Message.TextXAlignment = Enum.TextXAlignment.Left
Message.TextYAlignment = Enum.TextYAlignment.Top
Message.Parent = Main

--------------------------------------------------
-- LINK BOX
--------------------------------------------------

local LinkBox = Instance.new("Frame")
LinkBox.Size = UDim2.new(1, -54, 0, 45)
LinkBox.Position = UDim2.fromOffset(27, 158)
LinkBox.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
LinkBox.BorderSizePixel = 0
LinkBox.Parent = Main

local LinkCorner = Instance.new("UICorner")
LinkCorner.CornerRadius = UDim.new(0, 10)
LinkCorner.Parent = LinkBox

local LinkStroke = Instance.new("UIStroke")
LinkStroke.Color = Color3.fromRGB(50, 50, 50)
LinkStroke.Thickness = 1
LinkStroke.Parent = LinkBox

local LinkText = Instance.new("TextLabel")
LinkText.Size = UDim2.new(1, -24, 1, 0)
LinkText.Position = UDim2.fromOffset(12, 0)
LinkText.BackgroundTransparency = 1
LinkText.Text = DiscordLink
LinkText.TextColor3 = Color3.fromRGB(180, 180, 180)
LinkText.TextSize = 13
LinkText.Font = Enum.Font.GothamMedium
LinkText.TextXAlignment = Enum.TextXAlignment.Left
LinkText.Parent = LinkBox

--------------------------------------------------
-- COPY BUTTON
--------------------------------------------------

local Copy = Instance.new("TextButton")
Copy.Size = UDim2.new(1, -54, 0, 47)
Copy.Position = UDim2.fromOffset(27, 218)
Copy.BackgroundColor3 = Color3.fromRGB(15, 15, 15)
Copy.BorderSizePixel = 0
Copy.Text = "Copy Discord Invite"
Copy.TextColor3 = Color3.fromRGB(255, 255, 255)
Copy.TextSize = 14
Copy.Font = Enum.Font.GothamBold
Copy.AutoButtonColor = false
Copy.Parent = Main

local CopyCorner = Instance.new("UICorner")
CopyCorner.CornerRadius = UDim.new(0, 11)
CopyCorner.Parent = Copy

local CopyStroke = Instance.new("UIStroke")
CopyStroke.Color = Color3.fromRGB(65, 65, 65)
CopyStroke.Thickness = 1
CopyStroke.Parent = Copy

--------------------------------------------------
-- HOVERS
--------------------------------------------------

Copy.MouseEnter:Connect(function()
	TweenService:Create(
		Copy,
		TweenInfo.new(0.15),
		{BackgroundColor3 = Color3.fromRGB(30, 30, 30)}
	):Play()

	TweenService:Create(
		CopyStroke,
		TweenInfo.new(0.15),
		{Color = Color3.fromRGB(130, 130, 130)}
	):Play()
end)

Copy.MouseLeave:Connect(function()
	TweenService:Create(
		Copy,
		TweenInfo.new(0.15),
		{BackgroundColor3 = Color3.fromRGB(15, 15, 15)}
	):Play()

	TweenService:Create(
		CopyStroke,
		TweenInfo.new(0.15),
		{Color = Color3.fromRGB(65, 65, 65)}
	):Play()
end)

Close.MouseEnter:Connect(function()
	TweenService:Create(
		Close,
		TweenInfo.new(0.15),
		{
			BackgroundColor3 = Color3.fromRGB(30, 30, 30),
			TextColor3 = Color3.fromRGB(255, 255, 255)
		}
	):Play()
end)

Close.MouseLeave:Connect(function()
	TweenService:Create(
		Close,
		TweenInfo.new(0.15),
		{
			BackgroundColor3 = Color3.fromRGB(14, 14, 14),
			TextColor3 = Color3.fromRGB(165, 165, 165)
		}
	):Play()
end)

--------------------------------------------------
-- COPY
--------------------------------------------------

Copy.MouseButton1Click:Connect(function()
	if setclipboard then
		setclipboard(DiscordLink)

		Copy.Text = "✓ Invite Copied!"

		task.delay(1.5, function()
			if Copy and Copy.Parent then
				Copy.Text = "Copy Discord Invite"
			end
		end)
	else
		Copy.Text = DiscordLink

		task.delay(2, function()
			if Copy and Copy.Parent then
				Copy.Text = "Copy Discord Invite"
			end
		end)
	end
end)

--------------------------------------------------
-- CLOSE
--------------------------------------------------

Close.MouseButton1Click:Connect(function()
	local MainTween = TweenService:Create(
		Main,
		TweenInfo.new(
			0.2,
			Enum.EasingStyle.Quint,
			Enum.EasingDirection.In
		),
		{
			Size = UDim2.fromOffset(440, 250),
			BackgroundTransparency = 1
		}
	)

	TweenService:Create(
		Overlay,
		TweenInfo.new(0.2),
		{BackgroundTransparency = 1}
	):Play()

	MainTween:Play()
	MainTween.Completed:Wait()

	ScreenGui:Destroy()
end)

--------------------------------------------------
-- DRAGGING
--------------------------------------------------

local Dragging = false
local DragStart
local StartPosition

Main.InputBegan:Connect(function(Input)
	if Input.UserInputType == Enum.UserInputType.MouseButton1
		or Input.UserInputType == Enum.UserInputType.Touch then

		Dragging = true
		DragStart = Input.Position
		StartPosition = Main.Position
	end
end)

UserInputService.InputChanged:Connect(function(Input)
	if Dragging and (
		Input.UserInputType == Enum.UserInputType.MouseMovement
		or Input.UserInputType == Enum.UserInputType.Touch
	) then

		local Delta = Input.Position - DragStart

		Main.Position = UDim2.new(
			StartPosition.X.Scale,
			StartPosition.X.Offset + Delta.X,
			StartPosition.Y.Scale,
			StartPosition.Y.Offset + Delta.Y
		)
	end
end)

UserInputService.InputEnded:Connect(function(Input)
	if Input.UserInputType == Enum.UserInputType.MouseButton1
		or Input.UserInputType == Enum.UserInputType.Touch then

		Dragging = false
	end
end)

--------------------------------------------------
-- OPEN ANIMATION
--------------------------------------------------

Main.Size = UDim2.fromOffset(430, 245)
Main.BackgroundTransparency = 1

TweenService:Create(
	Overlay,
	TweenInfo.new(0.3),
	{BackgroundTransparency = 0.3}
):Play()

TweenService:Create(
	Main,
	TweenInfo.new(
		0.45,
		Enum.EasingStyle.Back,
		Enum.EasingDirection.Out
	),
	{
		Size = UDim2.fromOffset(500, 285),
		BackgroundTransparency = 0
	}
):Play()
